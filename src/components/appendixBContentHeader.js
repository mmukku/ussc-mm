import React from 'react';
import ContentHeader from '../components/contentHeader';

export default props => {
  return (
    <ContentHeader title="Appendix B" subtitle="Selected Sentencing Statutes" />
  );
};
